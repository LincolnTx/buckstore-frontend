import React from "react";
import { BrowserRouter, Switch, Route } from "react-router-dom";


const Routes = () => (
    <BrowserRouter> 
        <Switch>
            <h1>Hello World</h1>
        </Switch>
    </BrowserRouter>
);

export default Routes;